<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bildirishnomalar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <div class="my-3 d-flex justify-content-between align-items-center">
        <h1>Bildirishnomalar</h1>
        <% if (currentUser.role==="admin" ) { %>
            <a href="/todos/create" class="btn btn-primary" role="button">Bildirishnomalar Qo'shish</a>
            <% } %>
    </div>
    <table class="table table-hover table-bordered">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Tartib</th>
                <th scope="col">Foydalanuvchi</th>
                <th scope="col">Holat</th>
                <th scope="col">Amallar</th>
            </tr>
        </thead>
        <tbody>
            <% todos.forEach((todo, i)=> { %>
                <tr>
                    <th>
                        <%= i + 1 %>
                    </th>
                    <td>
                        <%= todo.guide ? todo.guide.title : 'Noma\' lum' %>
                    </td>
                    <td>
                        <%= `${todo.user.firstName} ${todo.user.lastName}` %>
                    </td>
                    <td>
                        <%= todo.completed ? "Tanishilgan" : "Tanishilmagan" %>
                    </td>
                    <td class="d-flex gap-3">
                        <a class="btn btn-success" href="/todos/<%= todo._id %>/show">Ko'rish</a>
                        <% if (currentUser.role==="admin" ) { %>
                            <form action="/todos/<%= todo._id %>/delete" method="post">
                                <button class="btn btn-danger" type="submit">O'chirish</button>
                            </form>
                            <% } %>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>